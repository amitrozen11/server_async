doctype html
html
    head
        title= title
        link(rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap")
    body
        h1 All Users
        ul
            each user in users
                li #{user.first_name} #{user.last_name} - ID: #{user.id}
        br
        a(href='/') Back to Home
